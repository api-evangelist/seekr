overlay: 1.0.0
info:
  title: API Evangelist enhancements for SeekrFlow — Explainability API
  version: 1.0.0
extends: openapi/seekr-explainability-openapi.json
x-generated: '2026-08-05'
x-method: generated
x-source: derived from openapi/seekr-explainability-openapi.json — the harvested document is never mutated
x-summary: Adds 0 untagged operations to a capability tag, marks 0 legacy-tagged operations, and flags 8 operations
  Seekr itself tags Internal.
actions:
- target: $.info
  description: Record where API Evangelist harvested this document and what surface it describes.
  update:
    x-apievangelist-profile: https://apis.io/provider/seekr/
    x-apievangelist-source: 'https://docs.seekr.com/mcp (query_docs_filesystem_seekr: /openapi/explainability.json)'
    x-apievangelist-harvested: '2026-08-05'
    x-api-surface: Explainability API
    x-apievangelist-description: SeekrFlow Explainability API. Authenticate with an API key in the Authorization
      header with NO Bearer prefix; scope every request to a team with the x-team-id header (omitting it targets
      the caller's personal workspace).
- target: $.info
  description: The published contact/termsOfService URLs use http:// and a generic support path; record the https
    equivalents we verified.
  update:
    x-apievangelist-docs: https://docs.seekr.com/flow/reference/getting-started-with-your-api
    x-apievangelist-status-page: https://status.seekr.com/
    x-apievangelist-changelog: https://docs.seekr.com/flow/changelog/seekr-managed/2026
- target: $.paths['/version'].get
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/'].get
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/status'].get
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/create-index'].post
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/populate-index'].post
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/delete-index'].post
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/influential-finetuning-data'].get
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
- target: $.paths['/v1/explainability/populate-index-job-status'].get
  description: Seekr tags this operation 'internal' — flag it so public consumers do not build on it.
  update:
    x-apievangelist-audience: internal
